<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ktu.solution.ilpinnovations.tcs.ktucopyscanner">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".activitites.SignInActivity"
            android:screenOrientation="reverseLandscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activitites.HomeActivity"
            android:screenOrientation="reverseLandscape">

        </activity>
        <activity android:name=".activitites.QRCodeScannerActivity" />
        <activity
            android:name=".activitites.CopyScannerActivity"
            android:screenOrientation="reverseLandscape" />
        <activity
            android:name=".activitites.QrcodeActivity"
            android:screenOrientation="reverseLandscape" />
        <activity android:name=".activitites.DisplayCopiesActivity" />

    </application>

</manifest>